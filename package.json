{
  "name": "portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "build": "vite build",
    "build:host": "cross-env IS_HOST=true vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "gh-pages": "^5.0.0",
    "sass": "^1.57.1",
    "typescript": "^5.6.3",
    "vite": "^4.0.4",
    "vite-plugin-html-inject": "^1.0.1"
  }
}
